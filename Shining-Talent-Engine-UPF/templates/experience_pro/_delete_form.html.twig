<form method="post" action="{{ path('experience_pro_delete', {'id': experience_pro.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience_pro.id) }}">
    <button class="btn">Delete</button>
</form>
